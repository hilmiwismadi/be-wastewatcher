# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=wastewatcher_db
DB_USER=postgres
DB_PASSWORD=postgres

# Server Configuration
PORT=5000
NODE_ENV=development

# JWT Configuration
JWT_SECRET=ww_production_jwt_secret_2025_capstone_secure_key_change_this
JWT_EXPIRES_IN=24h

# CORS Configuration
FRONTEND_URL=http://localhost:3003

# API Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# MQTT Configuration
# Note: test.mosquitto.org can be unreliable with connection resets and timeouts
# Alternative public brokers:
# - mqtt://broker.hivemq.com:1883 (HiveMQ public broker)
# - mqtt://broker.emqx.io:1883 (EMQX public broker)
# - mqtt://localhost:1883 (Local Mosquitto - recommended for production)
MQTT_BROKER_URL=mqtt://test.mosquitto.org:1883
MQTT_TOPIC=CapsE6/KantinSGLC
# MQTT_USERNAME=your-username
# MQTT_PASSWORD=your-password

# This was inserted by `prisma init`:
# Environment variables declared in this file are NOT automatically loaded by Prisma.
# Please add `import "dotenv/config";` to your `prisma.config.ts` file, or use the Prisma CLI with Bun
# to load environment variables from .env files: https://pris.ly/prisma-config-env-vars.

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

# Prisma Database URL (using existing PostgreSQL database)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/wastewatcher_db?schema=public"