const express = require('express');
const router = express.Router();

// Placeholder for alert routes - basic implementation
router.get('/', (req, res) => {
  res.json({ message: 'Alert routes - to be implemented' });
});

module.exports = router;