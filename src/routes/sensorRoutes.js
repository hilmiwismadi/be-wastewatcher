const express = require('express');
const router = express.Router();

// Placeholder for sensor routes - basic implementation
router.get('/', (req, res) => {
  res.json({ message: 'Sensor routes - to be implemented' });
});

module.exports = router;